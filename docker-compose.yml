services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ratkings
      POSTGRES_USER: ratkings
      POSTGRES_PASSWORD: ratkings
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
